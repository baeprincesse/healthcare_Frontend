import { Navigate, Outlet, useLocation } from "react-router-dom";
import { useAuth } from "../context/AuthContext.jsx";
export default function RoleProtectedRoute({roles=[]}){ const {authenticated,user}=useAuth(); const location=useLocation(); if(!authenticated)return <Navigate to="/login" state={{from:location}} replace/>; if(!roles.includes(user?.role)){ const p=user?.role==="SUPER_ADMINISTRATOR"?"/platform-admin/dashboard":user?.role==="HEAD_ADMINISTRATOR"?"/hospital-admin/dashboard":user?.role==="DOCTOR"?"/doctor/dashboard":"/dashboard"; return <Navigate to={p} replace/>;} return <Outlet/>; }
